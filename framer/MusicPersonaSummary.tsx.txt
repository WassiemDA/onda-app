type MusicPersonaSummaryProps = {
    summary: string
    userName?: string
}

export function MusicPersonaSummary({
    summary,
    userName,
}: MusicPersonaSummaryProps) {
    return (
        <div style={{ marginBottom: "32px" }}>
            <h2 style={{ fontSize: "24px", marginBottom: "8px" }}>
                🎧{" "}
                {userName
                    ? `${userName}'s Music Persona`
                    : "Your Music Persona"}
            </h2>
            <p>{summary}</p>
        </div>
    )
}
